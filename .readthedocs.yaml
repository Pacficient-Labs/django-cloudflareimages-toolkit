# Read the Docs configuration file for django-cloudflareimages-toolkit
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

version: 2

# Set the OS, Python version and other tools you might need
build:
  os: ubuntu-24.04
  tools:
    # 3.12 covers every Django version the package supports (4.2 / 5.1 /
    # 5.2 / 6.0). Bumped from 3.11 alongside the v1.0.12 metadata refresh.
    python: "3.12"

# Build documentation in the "docs/" directory with Sphinx
sphinx:
  configuration: docs/conf.py
  # TODO: flip back to ``true`` once docs/api.rst is scrubbed. The
  # current api.rst carries ~17 stale autodoc directives pointing at
  # symbols that don't exist (views.cloudflare_webhook,
  # apps.DjangoCloudflareimagesToolkitConfig, the entire utils module)
  # plus duplicate object descriptions vs usage.rst. Tracked as a
  # follow-up so v1.0.13 doesn't regress the RTD build.
  fail_on_warning: false

# Optionally build your docs in additional formats such as PDF and ePub
formats:
  - pdf
  - epub

# Optional but recommended, declare the Python requirements required
# to build your documentation
# See https://docs.readthedocs.io/en/stable/guides/reproducible-builds.html
python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
    - requirements: docs/requirements.txt
